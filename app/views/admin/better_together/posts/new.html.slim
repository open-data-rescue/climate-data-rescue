- provide(:better_together, "active")
- provide(:better_together_posts, "active")

h1 = t('better-together.community.posts.new')

== render 'form'

= link_to BetterTogether::Post.model_name.human(count: 2), admin_better_together_posts_path
