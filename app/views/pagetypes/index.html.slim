h3 Available Pagetypes
/! List all pagetypes and their info
table
  thead
    tr
      th Title
      th Author
      th External URL
      th
      - if current_user.admin?
        th
        th

  tbody
    - @pagetypes.each do |pagetype|
      tr
        td = link_to pagetype.title, pagetype
        td = pagetype.author
        td = pagetype.extern_ref
        td = link_to 'Show', pagetype
        - if current_user.admin?
          td = link_to 'Edit', edit_pagetype_path(pagetype)
          td = link_to 'Delete', pagetype, data: {:confirm => 'Are you sure?'}, :method => :delete

br

- if current_user.admin?
  = link_to 'New Pagetype', new_pagetype_path
