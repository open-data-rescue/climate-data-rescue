<div class="nav navbar navbar-default draw-footer-nav-container">
    <% if StaticPage.footer_links.any? %>
        <div class="footer-nav-wrapper">
            <ul class="nav navbar-nav footer-nav footer-nav-collapse">
                <% StaticPage.footer_links.each do |sp| %>
                    <%= render "static_pages/link", page: sp, top_level: true %>
                <% end %>
            </ul>
        </div>
    <% end %>
    <div>
        <p class="footer-copyright">&copy; <%= t('app.footer.copyright-holder') %> <%= Date.today.year %></p>
    </div>
</div>
